language: ruby
before_install:
  - gem update --system 1.8.23
  - gem --version
install:
  - ./ci/install-deps.sh
script:
  - ./ci/run-tests.sh
matrix:
  include:
    - rvm: 1.8.7
      env: TESTS=webui
    - rvm: 2.1.0
      env: TESTS=chef
branches:
  only:
    - master
