%h1= t('.title')

%table.data.box
  %thead
    %tr
      %th= t('.name')
      %th= t('.units')
      %th= t('.ram')
      %th= t('.drives')
      %th= t('.nics')
      %th= t('.cpu')
      %th= t('.hardware')
  %tbody
    - @classes.each do |n, c|
      %tr{ :class => cycle(:odd, :even, :name => "class") }
        %td
          %ul.plain
            - c[:names].each do |node|
              %li= link_to node[:alias], node_path(node[:handle]), :title=> node[:description]
        %td= c[:names].length
        %td= format_memory(c[:class][:ram])
        %td= c[:class][:drives]
        %td= c[:class][:nics]
        %td= c[:class][:cpu]
        %td= c[:class][:hw]
